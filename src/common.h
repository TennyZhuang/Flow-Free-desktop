#ifndef COMMON
#define COMMON
#include <QObject>

enum class Color
{
    NONE = 0,
    RED,
    GREEN,
    YELLOW,
    BLUE,
    ORANGE
};

const quint32 SCEAN_SIZE = 400;
const quint32 SCEAN_PADDING = 10;

#endif // COMMON

